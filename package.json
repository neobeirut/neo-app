{
  "name": "neo-app-root",
  "private": true,
  "description": "Root package.json for monorepo build detection",
  "scripts": {
    "postinstall": "cd web && npm install",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start"
  }
}
