[build]
builder = "nixpacks"
installCommand = "export PUPPETEER_SKIP_DOWNLOAD=true && cd web && npm install"
buildCommand = "cd web && npm run build"

[deploy]
startCommand = "cd web && npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
